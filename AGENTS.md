# AGENTS.md — سودبان (Sudban)

> این فایل برای هر ایجنت هوش مصنوعی (یا توسعه‌دهنده) که روی این ریپو کار می‌کند نوشته شده.
> قبل از هر تغییری، بخش «ویکی دانش پروژه» را بخوان و بعد از هر تغییر معنایی رعایتش کن.

## معرفی سریع پروژه
سودبان (Sudban) — دستیار هوشمند تصمیم‌گیری قیمت و مدیریت حاشیه سود برای فروشندگان ایرانی.
مستندات محصول/معماری: `docs/PRD.md`, `docs/ARCHITECTURE.md`. اسکیمای دیتابیس: `prisma/schema.prisma`.
اجرا/بیلد سنگین (npm install، prisma migrate، build) باید خارج از کانتینر ایجنت، روی سرور واقعی انجام شود.

---

# ویکی دانش پروژه — دستورالعمل (Vault Recipe)

## این چیست
یک «ویکی دانش» زنده و append-friendly کنار کد، در `docs/wiki/`، که به‌جای re-scan کردن کل کدبیس در هر
مکالمه/سشن، خلاصه‌ی entity ها (مدل‌ها، سرویس‌ها، routeها) و concept ها (flowها، الگوها، تصمیم‌ها) را نگه
می‌دارد. هدف: هرچه پروژه رشد می‌کند (مدل جدید، engine جدید، route جدید)، این ویکی هم همراهش رشد کند —
مثل شاخک‌های یک اختاپوس که با بزرگ‌شدن بدن، هر شاخک هم خودش را گسترش می‌دهد، بدون اینکه از نو ساخته شود.

## ساختار پوشه
```
docs/wiki/
  schema.md      # قوانین این ویکی — خودت (ایجنت) نگه‌اش می‌داری
  overview.md    # یک‌نگاه کلی پروژه (≤۴۰۰ کلمه)
  index.md       # کاتالوگ همه صفحات، گروه‌بندی‌شده، با خلاصه یک‌خطی
  log.md         # تاریخچه append-only (جدیدترین در پایین)
  entities/      # هر entity مهم یک صفحه (مدل دیتا، service، router)
  concepts/      # patternها، flowها، قراردادها، تصمیم‌های معماری
  sources/       # (اختیاری) خلاصه فایل‌های خام کد که چندجا ارجاع داده می‌شوند
```

## شروع هر مکالمه/سشن
1. اول `docs/wiki/overview.md` و `docs/wiki/index.md` را بخوان (هزینه: ≤۸۰۰ توکن). هرگز کل کدبیس را re-scan نکن.
2. فقط صفحات مرتبط با تسک فعلی را drill-down کن (`entities/*.md`, `concepts/*.md`).

## وقتی کد را تغییر دادی — قانون اکید
هر بار که یک فایل پروژه را Write/Edit می‌کنی (خارج از `docs/wiki/` خودش):
1. تشخیص بده این تغییر روی کدام entity/concept اثر می‌گذارد.
2. صفحه‌ی مربوطه را به‌روز کن (فیلد جدید/حذف‌شده، امضای تابع، route جدید، dependency جدید، edge case).
3. اگر entity/concept کاملاً جدید و به‌اندازه کافی مهم است (قانون زیر)، صفحه جدید بساز و در `index.md` لینک بده.
4. یک خط در `log.md` اضافه کن: `## [YYYY-MM-DD] update | <خلاصه تغییر>`.

تغییرات trivial (typo، فرمت، rename بدون تغییر امضا) نیاز به آپدیت ویکی ندارند.
اگر چند فایل در یک مکالمه تغییر کردند، آپدیت‌های ویکی را یک‌بار در پایان مکالمه انجام بده.

## چه وقت صفحه جدید بساز
- فقط اگر **≥۳ جای دیگر** قرار است به آن لینک بدهند، یا concept مستقلی با حجم قابل‌توجه است.
- در ۹۰٪ موارد به‌جای فایل جدید، **صفحه موجود را آپدیت کن** (یک paragraph یا section اضافه).
- هرگز صفحه‌ی trivial نساز (wrapper یک‌خطی، utility ساده) — در صفحه پدر یک bullet بنویس.

## ساختار صفحه‌ی entity
```markdown
# {نام}

> یک‌خطی توضیح هدف.

## مسئولیت‌ها
- ...

## وابستگی‌ها
- [[concepts/xxx]] — چرا
- [[entities/yyy]] — چطور

## قراردادها / Edge cases
- ...

## منابع کد
- `path/to/file.ts:42` — کلاس اصلی
```

## قواعد لینک (Obsidian-style)
- `[[entities/user]]` بدون پسوند `.md`.
- نام فایل: lowercase + hyphen.
- wikilinkهای نمونه داخل بک‌تیک/code block لینک واقعی محسوب نمی‌شوند.

## Lint دوره‌ای (وقتی کاربر گفت "ویکی رو بازبینی کن")
1. orphan pages (بدون inbound link) — لینک بده یا حذف کن.
2. broken link — صفحه‌ی گمشده را بساز یا لینک را حذف کن.
3. تناقض بین صفحات — flag کن، بهترین حدس را انتخاب کن و در log ثبت کن.
4. صفحات کهنه (کد مرتبط تغییر کرده ولی صفحه آپدیت نشده) — بازخوانی و sync.

جزئیات کامل قوانین در `docs/wiki/schema.md`.

## Mirror روی GitHub Wiki (`docs/gh-wiki/`)
علاوه بر `docs/wiki/` (منبع حقیقت)، یک نسخه mirror برای GitHub Wiki native در `docs/gh-wiki/`
نگه‌داری می‌شود (چون GitHub Wiki یک ریپوی git جدا با namespace تخت است: `sudban.wiki.git`).

- بعد از هر آپدیت در `docs/wiki/entities/*` یا `docs/wiki/concepts/*`، صفحه‌ی متناظر در
  `docs/gh-wiki/` را هم به‌روز کن (نام‌گذاری GH: `Title-Case-With-Hyphens.md`، لینک‌ها
  `[[Display Name|Page-Name]]`).
- `docs/gh-wiki/Home.md` معادل `overview.md` + `index.md` است؛ `docs/gh-wiki/_Sidebar.md` ناوبری
  کناری را نگه می‌دارد.
- برای انتشار واقعی روی گیت‌هاب: یک‌بار Wiki ریپو را در تنظیمات ریپو فعال کن (Settings → Features →
  Wikis)، سپس `git clone https://github.com/massoudsh/sudban.wiki.git`، محتوای `docs/gh-wiki/*` را
  در آن کپی و push کن (نیازمند توکن با دسترسی نوشتن؛ جزئیات احراز هویت در تاریخچه پروژه).
